<!-- 遍历table -->
#macro ( table $table )
    #if($table)
    <div class="main-content">
        <table class="table table-bordered table-hover">
            #if($!table.thead)
                <thead>
                <tr>
                    #foreach($th in $!table.thead)
                        <th>$th</th>
                    #end
                </tr>
                </thead>
            #end
            <tbody>
                #foreach($tr in $!table.tbodyData)
                <tr>
                    #foreach($td in $tr)
                        <td>
                            #if($td.getClass().getName() == "com.alibaba.rocketmq.common.Table")
                                <!-- recursive -->
                                #table($td)
                            #else
                                $td
                            #end
                        </td>
                    #end
                </tr>
                #end
            </tbody>
        </table>
    </div>
    <!-- properties-->
        #if($!table.extData && $!table.extData.size() > 0)
            #foreach($entry in $!table.extData.entrySet())
            <p>$entry.key:$entry:value</p>
            #end
        #end
    #end
#end

<!-- 动态生成form表单 -->
#macro ( form $options $action )
<div class="main-content">

    <table class="table table-bordered table-hover">
        <tbody>
        <form action="$action" method="post">
            #foreach($option in $options)
                <tr>
                    <th>$option.longOpt:</th>
                    <td>
                        <input class="form-inline span2" type="text" name="$option.longOpt"
                            #if($!option.getValue() && $!option.getValue() != "") value='$!option.getValue()' #end/>
                        #if($option.required)
                            <font color="red">*</font>
                        #end&nbsp;&nbsp;<span>$option.description</span></td>
                </tr>
            #end
            <tr>
                <td colspan="2">
                    <center>
                        <button class="btn btn-success" type="submit">Submit</button>
                    </center>
                </td>
            </tr>
        </form>
        </tbody>
    </table>
</div>
#end

<!-- 动态生成form表单 -->
#macro ( add)

#end

<!-- 解析title字段 -->
#macro(parseTitleToLink)
    #set ($myTitle = $title.split(":"))
    #foreach($param in $!{myTitle})
        <li><a href="#">$param</a></li>
    #end
#end